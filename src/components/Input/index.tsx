import React from "react";

type Props = {};

const Input = (props: Props) => {
    return <input className="border border-green-500 p-2 w-full mr-2" placeholder="Car Name" />;
};

export default Input;
