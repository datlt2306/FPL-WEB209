import { Button } from "@/components";

type Props = {};

const Item = (props: Props) => {
    return (
        <li>
            Item <Button>Remove</Button>
        </li>
    );
};

export default Item;
