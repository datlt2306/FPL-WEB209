import React from "react";
import { Button, Input } from "..";

type Props = {};

const Form = (props: Props) => {
    return (
        <form>
            <Input />
            <Button />
        </form>
    );
};

export default Form;
